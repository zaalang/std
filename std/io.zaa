//
// std io
//

import std.error;

pub concept OutputStream
{
  requires<T> (T os, u8 *buf, usize len) {
    try
    {
      len += os.write(buf, len);
    }
    catch(std::error e)
    {
    }
  };
}
